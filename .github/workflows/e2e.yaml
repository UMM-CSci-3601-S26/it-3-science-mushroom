name: End-to-End

on: [push]

jobs:
  ng-e2e:
    runs-on: ubuntu-latest

    services:
      mongo:
        image: mongo:7
        ports:
          - '27017-27019:27017-27019'

    steps:
    - name: Checkout repository
      uses: actions/checkout@v6

    - name: Cache Gradle artifacts (downloaded JARs, the wrapper, and any downloaded JDKs)
      uses: actions/cache@v5
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
          ~/.gradle/jdks
        key: ${{ runner.os }}-gradle-${{ hashFiles('**/.gradle/') }}
        restore-keys: |
          ${{ runner.os }}-gradle-

    - name: Use Node.js 22
      uses: actions/setup-node@v6
      with:
        node-version: 22
        cache: 'npm'
        cache-dependency-path: ./client/package-lock.json

    - name: Install client dependencies
      run: npm ci
      working-directory: ./client

    - name: Start backend server
      run: ${{ github.workspace }}/server/gradlew run -p ${{ github.workspace }}/server/ &

    - name: Wait for backend to be ready
      run: npx wait-on --timeout 60000 http-get://localhost:4567/api/health
      working-directory: ./client

    - name: Run e2e tests
      uses: cypress-io/github-action@v7
      with:
        browser: electron
        command: npm run e2e
        working-directory: ./client
      env:
        CYPRESS_E2E_ADMIN_USER: ${{ secrets.CYPRESS_E2E_ADMIN_USER }}
        CYPRESS_E2E_ADMIN_PASSWORD: ${{ secrets.CYPRESS_E2E_ADMIN_PASSWORD }}
        CYPRESS_E2E_STAFF_USER: ${{ secrets.CYPRESS_E2E_STAFF_USER }}
        CYPRESS_E2E_STAFF_PASSWORD: ${{ secrets.CYPRESS_E2E_STAFF_PASSWORD }}
        CYPRESS_E2E_VIEWER_USER: ${{ secrets.CYPRESS_E2E_VIEWER_USER }}
        CYPRESS_E2E_VIEWER_PASSWORD: ${{ secrets.CYPRESS_E2E_VIEWER_PASSWORD }}
